# Data Source Configuration
# Valid options for DATA_SOURCE_TYPE: 'local' or 'github'
DATA_SOURCE_TYPE=local

# Local data directory configuration
# This is the directory relative to the project root where CSV files are stored
LOCAL_DATA_DIR=data

# GitHub Configuration (only used if DATA_SOURCE_TYPE=github)
# Repository owner and name
GITHUB_OWNER=organization-name
GITHUB_REPO=data-repository
GITHUB_BRANCH=main

# Path within the GitHub repository where CSV files are located (optional)
# Leave empty if files are in the repository root
GITHUB_PATH=data

# GitHub access token with repo scope (optional, but recommended for private repos)
GITHUB_TOKEN=ghp_your_github_personal_access_token

# Local directory where GitHub data will be cached
GITHUB_LOCAL_CACHE_DIR=.data/github

# GitHub webhook secret for securing the refresh-data endpoint (optional)
GITHUB_WEBHOOK_SECRET=your_webhook_secret
